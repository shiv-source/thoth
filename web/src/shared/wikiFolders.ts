// wikiFolders — the default scaffold folder set, shared by the Settings
// general tab and the chat "Save as note" picker. The server keeps these
// defaults whenever wiki_folders is unset, so the frontend mirrors them for
// pickers that run before (or without) settings.
export const DEFAULT_WIKI_FOLDERS = ['inbox', 'meetings', 'projects', 'links', 'setup', 'knowledge', 'todos', 'daily']
