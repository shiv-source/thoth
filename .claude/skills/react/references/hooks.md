# Hooks (web/src/hooks)

The hook index lives in **docs/frontend.md § Hooks** — the authoritative
table of every hook under `web/src/hooks/` (purpose, signature, canonical
file). Read it there, not here.

Stale if: a new hook appears without a docs/frontend.md § Hooks entry.
